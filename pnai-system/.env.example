# Optional: a real ERC-20 contract to treat as the Master-Token (read-only).
MASTER_TOKEN_ADDRESS=

# Optional: REAL fiat settlement. Requires YOUR OWN registered provider account.
SETTLEMENT_PROVIDER=
SETTLEMENT_API_KEY=
SETTLEMENT_API_URL=
