// cfg.mjs — configuration placeholder module.
// This file intentionally exposes no runtime values yet; it exists so the
// toolchain has a stable module to resolve. Replace the export with real
// configuration when wiring up environment-specific settings.
export {};